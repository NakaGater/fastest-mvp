# fastest-mvp

A Claude Code / Copilot CLI plugin that takes an idea from conversation
to a tested, production-grade web application through a structured
Discovery -> Design -> Build -> Verify -> Ship workflow.

See the feature branch PR for the full v0.1-v0.5 plugin implementation.
